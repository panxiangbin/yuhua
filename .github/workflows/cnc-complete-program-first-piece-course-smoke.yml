name: CNC complete program first-piece course smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-complete-program-first-piece-course-smoke.yml'
  workflow_dispatch:

jobs:
  complete-program-first-piece-course:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --no-save playwright@1.52.0
      - run: npx playwright install --with-deps chromium
      - name: JavaScript syntax
        run: node --check cnc/tests/mobile-complete-program-first-piece-course-smoke.cjs
      - name: Start static server
        run: python3 -m http.server 4173 --directory . > /tmp/cnc-server.log 2>&1 &
      - name: Chromium 390x844 complete program first-piece course
        run: node cnc/tests/mobile-complete-program-first-piece-course-smoke.cjs
      - name: Upload complete program first-piece diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: complete-program-first-piece-course-diagnostics
          path: artifacts/
          if-no-files-found: warn
          retention-days: 7