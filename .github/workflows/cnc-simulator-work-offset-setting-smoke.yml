name: CNC work offset setting simulator smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-simulator-work-offset-setting-smoke.yml'
  workflow_dispatch:

jobs:
  work-offset-setting-simulator:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --no-save playwright@1.52.0
      - run: npx playwright install --with-deps chromium
      - name: JavaScript syntax
        run: node --check cnc/tests/mobile-simulator-work-offset-setting-smoke.cjs
      - name: Start static server
        run: python3 -m http.server 4173 --directory . > /tmp/cnc-server.log 2>&1 &
      - name: Chromium 390x844 work offset setting simulator
        run: node cnc/tests/mobile-simulator-work-offset-setting-smoke.cjs
      - name: Upload work offset setting diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: work-offset-setting-simulator-diagnostics
          path: artifacts/simulator-work-offset-setting/
          if-no-files-found: warn
          retention-days: 7
