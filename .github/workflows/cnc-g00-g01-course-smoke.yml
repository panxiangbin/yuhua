name: CNC G00 G01 course smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-g00-g01-course-smoke.yml'
  workflow_dispatch:

jobs:
  g00-g01-course:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --no-save playwright@1.52.0
      - run: npx playwright install --with-deps chromium
      - name: JavaScript syntax
        run: node --check cnc/tests/mobile-g00-g01-course-smoke.cjs
      - name: Start static server
        run: python3 -m http.server 4173 --directory . > /tmp/cnc-server.log 2>&1 &
      - name: Chromium 390x844 G00 G01 course
        run: node cnc/tests/mobile-g00-g01-course-smoke.cjs
      - name: Upload G00 G01 diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g00-g01-course-diagnostics
          path: artifacts/
          if-no-files-found: warn
          retention-days: 7
