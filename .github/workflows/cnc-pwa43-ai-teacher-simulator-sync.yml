name: CNC PWA43 AI teacher simulator sync

on:
  push:
    branches:
      - codex/cnc-ai-teacher-simulator-pass-pwa43
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Correct temporary sync unique-file accounting
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p=Path('cnc/tests/pwa43-ai-teacher-simulator-sync.cjs')
          s=p.read_text(encoding='utf-8')
          old='  changed.push(file);\n  return true;'
          new='  if (!changed.includes(file)) changed.push(file);\n  return true;'
          assert s.count(old)==1, 'temporary sync accounting patch target mismatch'
          p.write_text(s.replace(old,new), encoding='utf-8')
          PY
      - name: Apply controlled AI teacher and PWA43 sync
        run: node cnc/tests/pwa43-ai-teacher-simulator-sync.cjs
      - name: Remove one-time sync tooling before strict audit
        shell: bash
        run: |
          set -euo pipefail
          git rm -f cnc/tests/pwa43-ai-teacher-simulator-sync.cjs .github/workflows/cnc-pwa43-ai-teacher-simulator-sync.yml
      - name: Run strict PWA build reference audit on final tree
        run: node cnc/tests/pwa-build-reference-audit-smoke.cjs
      - name: Verify AI teacher JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import re, pathlib
          src=pathlib.Path('cnc/ai-teacher.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script(?:\s[^>]*)?>([\s\S]*?)</script>', src, flags=re.I)
          pathlib.Path('/tmp/ai-teacher-inline.js').write_text('\n'.join(scripts), encoding='utf-8')
          PY
          node --check /tmp/ai-teacher-inline.js
          node --check cnc/tests/ai-teacher-data-integrity-smoke.cjs
      - name: Create generated final-tree commit
        id: generated
        shell: bash
        env:
          BASE_MAIN: 036d5e68e65d93586c54fb9c8b1e3179a81c9dca
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add cnc .github/workflows/cnc-*.yml
          git reset -- cnc/test-results || true
          git diff --cached --name-only | tee changed-files-from-temp-parent.txt
          test "$(git diff --cached --name-only | wc -l | tr -d ' ')" = '36'
          git diff --cached --name-only | awk '($0 ~ /^cnc\//) || ($0 ~ /^\.github\/workflows\/cnc-.*\.ya?ml$/) {next} {print "unexpected path: "$0; bad=1} END{exit bad}'
          git commit -m 'CNC：统一AI老师模拟满分通过并升级PWA43'
          git rev-parse HEAD | tee generated-sha.txt
          git diff --name-only "$BASE_MAIN" HEAD | tee final-diff-files.txt
          test "$(git diff --name-only "$BASE_MAIN" HEAD | wc -l | tr -d ' ')" = '34'
          ! grep -Fxq 'cnc/tests/pwa43-ai-teacher-simulator-sync.cjs' final-diff-files.txt
          ! grep -Fxq '.github/workflows/cnc-pwa43-ai-teacher-simulator-sync.yml' final-diff-files.txt
          git diff --name-only "$BASE_MAIN" HEAD | awk '($0 ~ /^cnc\//) || ($0 ~ /^\.github\/workflows\/cnc-.*\.ya?ml$/) {next} {print "unexpected final path: "$0; bad=1} END{exit bad}'
      - name: Attempt non-force branch push
        id: push
        shell: bash
        run: |
          set +e
          git push origin HEAD:codex/cnc-ai-teacher-simulator-pass-pwa43 > push-output.txt 2>&1
          code=$?
          echo "$code" > push-code.txt
          cat push-output.txt
          exit 0
      - name: Upload controlled sync diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cnc-pwa43-ai-teacher-simulator-sync-${{ github.run_id }}
          path: |
            cnc/test-results/pwa43-ai-teacher-simulator-sync/sync-report.json
            cnc/test-results/pwa-build-reference-audit/report.json
            changed-files-from-temp-parent.txt
            final-diff-files.txt
            generated-sha.txt
            push-code.txt
            push-output.txt
          retention-days: 7
          if-no-files-found: error
