name: CNC training camp route handoff Pages smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-training-camp-route-handoff-pages-smoke.yml'
  push:
    branches: [main]
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-training-camp-route-handoff-pages-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cnc-training-camp-route-handoff-pages-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Validate current build contract
        run: |
          mkdir -p cnc/test-results/training-camp-route-handoff-pages
          node --check cnc/tests/pages-training-camp-route-handoff-deployment-smoke.cjs
          node --check cnc/sw.js
          node - <<'NODE'
          const fs = require('fs');
          const info = JSON.parse(fs.readFileSync('cnc/build-info.json', 'utf8'));
          const sw = fs.readFileSync('cnc/sw.js', 'utf8');
          const file = 'cnc/tests/pages-training-camp-route-handoff-deployment-smoke.cjs';
          let test = fs.readFileSync(file, 'utf8');
          const swBuild = sw.match(/const BUILD = '([^']+)'/)?.[1];
          if (swBuild !== info.pwaBuild) throw new Error(`Service Worker/build-info mismatch: ${swBuild} / ${info.pwaBuild}`);
          if (!test.includes(`const expectedPwaBuild = '${info.pwaBuild}'`)) throw new Error('training camp deployment PWA target mismatch');
          test = test.replace(/const expectedSiteBuild = '[^']+';/, `const expectedSiteBuild = '${info.build}';`);
          fs.writeFileSync(file, test);
          for (const marker of ['80个图文小课', '手机首页一屏化']) {
            if (!String(info.contentStage || '').includes(marker)) throw new Error(`build marker missing: ${marker}`);
          }
          NODE
      - name: Verify main and Pages exact deployment
        env:
          CNC_PAGES_VERIFY_ATTEMPTS: 18
          CNC_PAGES_VERIFY_INTERVAL_MS: 10000
        run: node cnc/tests/pages-training-camp-route-handoff-deployment-smoke.cjs
      - name: Upload deployment diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cnc-training-camp-route-handoff-pages-${{ github.run_id }}
          path: cnc/test-results/training-camp-route-handoff-pages/**
          if-no-files-found: error
          retention-days: 14
