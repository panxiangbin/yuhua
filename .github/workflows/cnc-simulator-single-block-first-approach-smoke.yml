name: CNC single-block first-approach simulator smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-simulator-single-block-first-approach-smoke.yml'
  workflow_dispatch:

jobs:
  single-block-first-approach-simulator:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --no-save playwright@1.52.0
      - run: npx playwright install --with-deps chromium
      - name: JavaScript syntax
        run: node --check cnc/tests/mobile-simulator-single-block-first-approach-smoke.cjs
      - name: Start static server
        run: python3 -m http.server 4173 --directory . > /tmp/cnc-server.log 2>&1 &
      - name: Chromium 390x844 single-block first-approach simulator
        run: node cnc/tests/mobile-simulator-single-block-first-approach-smoke.cjs
      - name: Upload single-block first-approach diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: single-block-first-approach-simulator-diagnostics
          path: artifacts/simulator-single-block-first-approach/
          if-no-files-found: warn
          retention-days: 7
