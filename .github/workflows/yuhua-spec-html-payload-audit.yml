name: Yuhua specification HTML payload audit

on:
  pull_request:
    paths:
      - 'specs/**'
      - 'specs_index.json'
      - 'tools/audit_spec_html_payloads.py'
      - 'tools/audit_spec_inline_externalization.py'
      - 'tools/audit_top_spec_inline_candidate.py'
      - 'tools/audit_top_spec_inline_render_equivalence.py'
      - 'tools/verify_spec_externalization_integrity.py'
      - '.github/workflows/yuhua-spec-html-payload-audit.yml'
  push:
    branches: [main]
    paths:
      - 'specs/**'
      - 'specs_index.json'
      - 'tools/audit_spec_html_payloads.py'
      - 'tools/audit_spec_inline_externalization.py'
      - 'tools/audit_top_spec_inline_candidate.py'
      - 'tools/audit_top_spec_inline_render_equivalence.py'
      - 'tools/verify_spec_externalization_integrity.py'
      - '.github/workflows/yuhua-spec-html-payload-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit-spec-html-payloads:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Guard shared-image externalization integrity
        run: |
          python tools/verify_spec_externalization_integrity.py \
            --base-ref HEAD^1 \
            --json spec_externalization_integrity_report.json \
            --markdown spec_externalization_integrity_report.md

      - name: Install render-audit dependencies
        run: python -m pip install --disable-pip-version-check --quiet Pillow==11.3.0 selenium==4.35.0

      - name: Audit indexed specification HTML payloads
        run: |
          python tools/audit_spec_html_payloads.py \
            --json spec_html_payload_audit.json \
            --markdown spec_html_payload_audit.md

      - name: Audit inline image externalization feasibility
        run: |
          python tools/audit_spec_inline_externalization.py \
            --json spec_inline_externalization_audit.json \
            --markdown spec_inline_externalization_audit.md

      - name: Audit largest inline-image candidate context and projected savings
        run: |
          python tools/audit_top_spec_inline_candidate.py \
            --feasibility-json spec_inline_externalization_audit.json \
            --json spec_inline_top_candidate_context_audit.json \
            --markdown spec_inline_top_candidate_context_audit.md

      - name: Browser-check largest candidate render equivalence
        run: |
          python tools/audit_top_spec_inline_render_equivalence.py \
            --context-json spec_inline_top_candidate_context_audit.json \
            --json spec_inline_top_candidate_render_equivalence.json \
            --markdown spec_inline_top_candidate_render_equivalence.md \
            --screenshots-dir "$RUNNER_TEMP/yuhua-spec-render-equivalence-screenshots"

      - name: Verify report integrity
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          integrity = json.loads(Path('spec_externalization_integrity_report.json').read_text(encoding='utf-8'))
          assert integrity['ok'], integrity['errors']
          assert integrity['verified_replacement_count'] >= 0
          assert '不修改规格书、技术参数、型号含义或产品数据' in integrity['policy']

          report = json.loads(Path('spec_html_payload_audit.json').read_text(encoding='utf-8'))
          assert report['index_entry_count'] > 0
          assert report['audited_existing_page_count'] + report['missing_indexed_page_count'] == report['index_entry_count']
          assert report['indexed_unique_page_count'] <= report['indexed_page_path_count']
          assert report['tracked_spec_html_count'] >= report['indexed_unique_page_count'] - report['indexed_but_untracked_html_count']
          assert report['total_inline_data_uri_bytes'] >= report['total_inline_encoded_payload_bytes'] >= 0
          assert report['total_inline_image_occurrences'] >= report['pages_with_inline_images'] >= 0
          assert '不会压缩、删除、提取、重写或替换' in report['policy']

          feasibility = json.loads(Path('spec_inline_externalization_audit.json').read_text(encoding='utf-8'))
          assert feasibility['tracked_spec_html_count'] == report['tracked_spec_html_count']
          assert feasibility['total_inline_image_occurrences'] == report['total_inline_image_occurrences']
          assert (
              feasibility['inline_img_src_occurrence_count']
              + feasibility['inline_non_img_src_occurrence_count']
              == feasibility['total_inline_image_occurrences']
          )
          assert (
              feasibility['strict_mechanical_candidate_group_count']
              + feasibility['rejected_duplicate_group_count']
              == feasibility['exact_duplicate_payload_group_count']
          )
          assert all(row['strict_mechanical_candidate'] for row in feasibility['strict_candidates'])
          assert '不代表已获准修改资料' in feasibility['policy']

          top = json.loads(Path('spec_inline_top_candidate_context_audit.json').read_text(encoding='utf-8'))
          assert feasibility['strict_candidates'], 'top-candidate context audit requires a strict candidate'
          expected = feasibility['strict_candidates'][0]
          assert top['selected_candidate_sha256'] == expected['sha256']
          assert top['candidate_shared_path'] == expected['candidate_shared_path']
          assert top['page_count'] == expected['page_count']
          assert top['occurrence_count'] == expected['occurrences'] == top['expected_occurrence_count']
          assert len(top['occurrences']) == top['occurrence_count']
          assert {row['page'] for row in top['occurrences']} == set(expected['pages'])
          assert all(row['encoded_payload_sha256'] == top['selected_candidate_sha256'] for row in top['occurrences'])
          assert top['decoded_image_bytes'] > 0
          assert top['projected_affected_html_bytes_after_reference_swap'] < top['current_affected_html_bytes']
          assert top['projected_combined_bytes_with_one_shared_file'] < top['current_affected_html_bytes']
          assert top['projected_net_bytes_reduction'] > 0
          assert '不证明各页面语义用途一致' in top['policy']

          render = json.loads(Path('spec_inline_top_candidate_render_equivalence.json').read_text(encoding='utf-8'))
          assert render['selected_candidate_sha256'] == top['selected_candidate_sha256']
          assert render['decoded_image_sha256'] == top['decoded_image_sha256']
          assert render['candidate_shared_path'] == top['candidate_shared_path']
          assert render['occurrence_count'] == top['occurrence_count']
          assert render['viewport_count'] == 2
          assert render['comparison_case_count'] == render['occurrence_count'] * render['viewport_count']
          assert render['all_geometry_equivalent']
          assert render['all_element_screenshots_pixel_identical']
          assert all(case['geometry_identical_with_tolerance'] for case in render['cases'])
          assert all(case['pixel_identical'] for case in render['cases'])
          assert all(case['same_dimensions'] for case in render['cases'])
          assert '不会修改任何受版本控制的规格书' in render['policy']
          print('specification externalization integrity, payload, feasibility, context, and browser render-equivalence reports: OK')
          PY

      - name: Upload specification HTML payload audit
        uses: actions/upload-artifact@v4
        with:
          name: yuhua-spec-html-payload-audit
          path: |
            spec_externalization_integrity_report.json
            spec_externalization_integrity_report.md
            spec_html_payload_audit.json
            spec_html_payload_audit.md
            spec_inline_externalization_audit.json
            spec_inline_externalization_audit.md
            spec_inline_top_candidate_context_audit.json
            spec_inline_top_candidate_context_audit.md
            spec_inline_top_candidate_render_equivalence.json
            spec_inline_top_candidate_render_equivalence.md
            ${{ runner.temp }}/yuhua-spec-render-equivalence-screenshots/
          retention-days: 90
