name: CNC G50 multi semantic runtime trust smoke

on:
  pull_request:
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-g50-multi-semantic-runtime-trust-smoke.yml'
  push:
    branches:
      - main
    paths:
      - 'cnc/**'
      - '.github/workflows/cnc-g50-multi-semantic-runtime-trust-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trust:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
      CNC_G50_REPORT: cnc-g50-multi-semantic-runtime-trust-report.json
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.EXPECTED_HEAD }}
          fetch-depth: 1

      - name: Verify exact checkout
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run G50 multi-semantic runtime trust gate
        run: node cnc/tests/g50-multi-semantic-runtime-trust-smoke.cjs

      - name: Upload G50 trust diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cnc-g50-multi-semantic-runtime-trust-${{ github.run_id }}
          path: cnc-g50-multi-semantic-runtime-trust-report.json
          if-no-files-found: error
          retention-days: 14
