name: CNC mobile home compatibility smoke

on:
  pull_request:
    paths:
      - 'cnc/index.html'
      - 'cnc/personal-home.js'
      - 'cnc/homepage-refresh.css'
      - 'cnc/homepage-refresh-desktop-legacy.css'
      - 'cnc/mobile-home-refactor.css'
      - 'cnc/mobile-trust-nav.js'
      - 'cnc/sw.js'
      - 'cnc/build-info.json'
      - 'cnc/featured-images*.js'
      - 'cnc/assets/images/**'
      - 'cnc/course-*.html'
      - 'cnc/tests/mobile-home-refactor-smoke.cjs'
      - '.github/workflows/cnc-mobile-home-game-ui-smoke.yml'
  push:
    branches:
      - main
    paths:
      - 'cnc/index.html'
      - 'cnc/personal-home.js'
      - 'cnc/homepage-refresh.css'
      - 'cnc/homepage-refresh-desktop-legacy.css'
      - 'cnc/mobile-home-refactor.css'
      - 'cnc/mobile-trust-nav.js'
      - 'cnc/sw.js'
      - 'cnc/build-info.json'
      - 'cnc/featured-images*.js'
      - 'cnc/assets/images/**'
      - 'cnc/course-*.html'
      - 'cnc/tests/mobile-home-refactor-smoke.cjs'
      - '.github/workflows/cnc-mobile-home-game-ui-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cnc-mobile-home-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  mobile-home:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validate mobile refactor syntax and contracts
        run: |
          set -euxo pipefail
          test -f cnc/mobile-home-refactor.css
          test -f cnc/homepage-refresh-desktop-legacy.css
          test -f cnc/tests/mobile-home-refactor-smoke.cjs
          test -f cnc/MOBILE_HOME_REFACTOR_PROGRESS.md
          test -f cnc/MOBILE_HOME_ASSET_INVENTORY.md
          node --check cnc/personal-home.js
          node --check cnc/tests/mobile-home-refactor-smoke.cjs
          grep -q '速肯方面' cnc/MOBILE_HOME_ASSET_INVENTORY.md
          grep -q '360×800' cnc/MOBILE_HOME_REFACTOR_PROGRESS.md
          grep -q '教学参考，需按机床说明书、现场工艺和空运行验证' cnc/MOBILE_HOME_ASSET_INVENTORY.md || true
          ! grep -Eq "test\.skip\(|describe\.skip\(|it\.skip\(|process\.exit\(0\)" cnc/tests/mobile-home-refactor-smoke.cjs

      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.54.2
          npx playwright install --with-deps chromium

      - name: Run first-paint, one-screen, learning, query, PWA and desktop checks
        run: node cnc/tests/mobile-home-refactor-smoke.cjs

      - name: Upload mobile refactor evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cnc-mobile-home-compatibility-${{ github.run_id }}
          path: cnc/test-results/mobile-home-refactor/
          if-no-files-found: error
          retention-days: 30
