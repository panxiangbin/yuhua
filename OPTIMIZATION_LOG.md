# 予华仪器网站优化记录

## 2026-07-28 第一批改造

- 建立独立分支 `upgrade-84h`，不直接改动主分支。
- 自动隐藏型号和名称同时为空、待审核、停用的数据。
- 搜索范围扩展到型号、名称、类别、容量、材质、温度、压力、真空度、功率、用途、特点及 `specs` 字段。
- 增加容量、材质、温度高级筛选。
- 手机端型号表格改为产品卡片。
- 删除“匹配不到型号时跳到同分类其他详情页”的危险兜底。
- 增加按应用场景找产品与智能选型 V1。
- 增加统一联系方式配置 `assets/site-config.js`。
- 增加 SEO 元数据、`robots.txt`、`sitemap.xml` 和 `404.html`。
- 增加轻量 CI 检查，防止数据文件失效、关键 DOM 缺失和错误资料兜底回归。

## 后续重点

- 对产品数据库进行逐型号来源核对，不让自动程序猜参数。
- 为重点型号建立专属详情页与明确的 PDF/Word 绑定。
- 增加工厂实拍、资质与真实案例素材。
- 将智能选型规则扩展到物料兼容性、热负荷、真空抽气时间和配套设备计算。
