# upgrade-84h 合并阻断审计

审计日期：2026-08-01

工作区：`upgrade-84h` 分支、草稿 PR #96

## 结论

PR #96 当前仍为开放、草稿、未合并状态，`main` 与线上站点未被修改。

分支头提交 `2decf41301ae385e4046d349a842ed8de9726cac` 对应的现有自动测试已经全部完成并通过；公开联系方式回归审计也通过，最新扫描 1404 个公开文本文件，发现问题 0、受影响文件 0，未发现手机号、微信号、可执行拨号链接、复制号码控件或其他直接联系方式入口。

但在最终报告完成后，`main` 已前进到 `4190b7feccb9b809ec405b1565404e74480a3e58`，相对共同基点 `ca5a8e7f2fb588f4b126719422701633afa4e95d` 新增 14 个提交。GitHub 当前将 PR #96 标记为不可自动合并，因此“可安全上线”条件已不再满足。

## 重叠改动范围

`main` 的新增提交与 PR #96 在以下 12 个文件上存在重叠修改：

1. `.github/workflows/cnc-mobile-quality.yml`
2. `.github/workflows/cnc-pages-deployment-status-smoke.yml`
3. `.github/workflows/cnc-pwa-offline-cache-smoke.yml`
4. `cnc/build-info.json`
5. `cnc/pwa-self-test.html`
6. `cnc/pwa-status.html`
7. `cnc/sw.js`
8. `cnc/tests/mobile-pages-deployment-status-smoke.cjs`
9. `cnc/tests/mobile-pwa-offline-cache-smoke.cjs`
10. `cnc/tests/mobile-pwa-profile-bfcache-smoke.cjs`
11. `cnc/tests/mobile-pwa-self-test-smoke.cjs`
12. `cnc/tests/pwa-controller-test-helper.cjs`

这些文件涉及 Service Worker、PWA 版本、离线缓存、BFCache、Pages 状态和手机端测试。最新 `main` 同时新增了 CNC 手机端无障碍、AI 教师、起点测评、PWA 数据升级等功能及回归门禁。

## 本轮未强行解决的原因

本轮没有采用“全部保留分支”或“全部保留 main”的自动冲突策略：

- 全部保留分支可能覆盖 `main` 新增的 CNC 功能、测试和稳定性修复；
- 全部保留 `main` 可能丢失 PR #96 已验证的 PWA 离线、BFCache 和诊断修复；
- 在没有完整三方合并、逐文件检查和合并后浏览器回归的情况下，强行生成合并提交不属于安全改动。

因此本轮只记录阻断事实和复核范围，没有修改产品型号、压力、温度、材质、详情参数或业务数据。

## 解除阻断所需步骤

1. 在具备仓库写权限和完整 Git 工作区的环境中，将最新 `main` 合并或变基到 `upgrade-84h`。
2. 对上述 12 个重叠文件逐项合并，优先保留 `main` 新增功能，同时保留 PR #96 已验证的离线回退、BFCache、版本一致性和诊断能力。
3. 重新运行全部现有 GitHub Actions，而不是只运行局部测试。
4. 再次执行公开联系方式策略与泄漏审计。
5. 确认 PR 恢复可合并、手机端和桌面端关键流程全绿后，才可重新判断是否达到完成标准。
6. 未获得用户明确上线授权前，继续保持 PR 为草稿，不得合并到 `main`。

## 当前发布状态

- PR #96：开放、草稿、未合并、当前不可自动合并。
- `upgrade-84h`：保留全部网站优化、最终报告和本次阻断审计。
- `main`：未修改。
- 线上站点：未修改。
