const { chromium }=require('playwright');const {spawn}=require('child_process');const assert=require('assert');const fs=require('fs');const path=require('path');(async()=>{const root=path.resolve(__dirname,'../..');const server=spawn('python3',['-m','http.server','4173'],{cwd:root,stdio:'ignore'});let browser;try{browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:390,height:844}});const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await page.goto('http://127.0.0.1:4173/cnc/profile.html');await page.evaluate(()=>{localStorage.setItem('cnc_training_profile_v1',JSON.stringify({version:1,xp:420,abilities:{safety:88,coordinate:76,drawing:82,programVerification:91,measurementDiagnosis:70,troubleshooting:64}}));localStorage.setItem('cnc_training_practice_v1',JSON.stringify({version:1,history:[{practiceId:'safety-coordinate',score:100},{practiceId:'advanced-verification',score:87},{practiceId:'drawing-setup-process',score:67}],wrongQuestions:[{id:'a1',practiceId:'advanced-verification',ability:'程序验证',risk:'高'},{id:'a2',practiceId:'drawing-setup-process',ability:'图纸识读',risk:'中'},{id:'a3',practiceId:'alarm-parameter-first-piece',ability:'故障排查',risk:'高'}]}));localStorage.setItem('cnc_training_simulator_v1',JSON.stringify({version:1,simulators:{homing:{passed:true,bestScore:100},workholding:{passed:true,bestScore:100},alarm:{passed:false,bestScore:75}}}));});await page.reload();await page.waitForFunction(()=>document.querySelector('#xp')?.textContent==='420');assert.equal(await page.locator('#practice-pass').textContent(),'2/5');assert.equal(await page.locator('#wrong-count').textContent(),'3');assert.equal(await page.locator('#sim-pass').textContent(),'2/13');assert.equal(await page.locator('#recommend-practice').getAttribute('href'),'./practice-wrong-review.html');assert((await page.locator('#ability-grid .ability').count())===6);const min=await page.locator('a:visible').evaluateAll(ns=>Math.min(...ns.map(n=>n.getBoundingClientRect().height)));assert(min>=44,`touch target ${min}`);assert.equal(errors.length,0,errors.join('\n'));fs.mkdirSync(path.join(root,'cnc/test-results'),{recursive:true});await page.screenshot({path:path.join(root,'cnc/test-results/profile-practice-analytics-390x844.png'),fullPage:true});fs.writeFileSync(path.join(root,'cnc/test-results/profile-practice-analytics.json'),JSON.stringify({xp:420,practicePass:'2/5',wrong:3,simPass:'2/13',minTouch:min},null,2));console.log('profile analytics smoke passed')}finally{if(browser)await browser.close();server.kill('SIGTERM')}})().catch(e=>{console.error(e);process.exit(1)});