const{chromium}=require('playwright');const{spawn}=require('child_process');const assert=require('assert');const fs=require('fs');const path=require('path');(async()=>{const root=path.resolve(__dirname,'../..');const server=spawn('python3',['-m','http.server','4173'],{cwd:root,stdio:'ignore'});let browser;try{browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:390,height:844}});const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await page.goto('http://127.0.0.1:4173/cnc/data-health.html');const originalDone=JSON.stringify([1,2,'stage-3','4',13,-1,null,[]]);await page.evaluate(done=>{localStorage.clear();localStorage.setItem('cnc_training_profile_v1',JSON.stringify({xp:420,lastTrainingDate:'2026-02-30',trainingDays:['2026-08-12','2026-08-12','2026-02-30',null,7,{},[]],badges:['连续训练3天',' 连续训练3天 ',null,{}]}));localStorage.setItem('cnc_training_practice_v1',JSON.stringify({version:1,wrongQuestions:{a:{id:'w1'},b:{id:'w2'}},wrongItems:{c:{id:'w3'}},wrong:{d:{id:'w4'}},lessonScores:{1:100,2:'80',13:90,3:120}}));localStorage.setItem('cnc_training_simulator_v1',JSON.stringify({homing:{passed:true,bestScore:100},workholding:{passed:false,bestScore:50}}));localStorage.setItem('cnc_study_completed_v1',done);localStorage.setItem('cnc_training_backup_meta_v1',JSON.stringify({lastExportAt:'2026-02-30T00:00:00Z'}));localStorage.setItem('unrelated_keep_me','保留')},originalDone);await page.reload();await page.waitForFunction(()=>document.querySelectorAll('#checks .check').length===11);assert.equal(await page.locator('#data-count').textContent(),'4');assert(Number(await page.locator('#warn-count').textContent())>=5);assert((await page.locator('#checks').textContent()).includes('备份时间无效'),'不存在的备份日期必须提示重新导出');const trainingDayCheck=page.locator('#checks .check').filter({hasText:'训练日记录'});assert.equal(await trainingDayCheck.count(),1);const trainingDayText=await trainingDayCheck.textContent();assert(trainingDayText.includes('5个无法确认的训练日条目'),'必须识别不存在日期、null、数字、对象与数组等5个异常训练日条目');assert(trainingDayText.includes('不会自动删除或改写训练日记录'),'训练日异常只能提示，不得静默清洗');const practiceScoreCheck=page.locator('#checks .check').filter({hasText:'练习成绩结构'});assert.equal(await practiceScoreCheck.count(),1);const practiceScoreText=await practiceScoreCheck.textContent();assert(practiceScoreText.includes('3个无法确认的课程练习成绩'),'必须识别数字字符串、越界关卡和越界分数等3个异常练习成绩');assert(practiceScoreText.includes('不会自动修改成绩'),'练习成绩异常只能提示，不得静默清洗');const wrongCheck=page.locator('#checks .check').filter({hasText:'错题结构'});assert.equal(await wrongCheck.count(),1);const wrongText=await wrongCheck.textContent();assert(wrongText.includes('3个对象型错题兼容字段'),'wrongQuestions / wrongItems / wrong 三类对象字段必须全部识别');const rewardCheck=page.locator('#checks .check').filter({hasText:'奖励记录风险'});assert.equal(await rewardCheck.count(),1);const rewardText=await rewardCheck.textContent();assert(rewardText.includes('提醒'),'重复训练日/徽章必须触发奖励风险提醒');assert(rewardText.includes('3类奖励记录风险'),'必须同时识别重复训练日、重复徽章和非法徽章条目');assert(rewardText.includes('2个徽章条目无法确认'),'null 与对象型徽章必须明确告警');assert(rewardText.includes('不会自动删除奖励记录'),'奖励风险只能提示，不得静默清洗');const completionCheck=page.locator('#checks .check').filter({hasText:'课程完成记录'});assert.equal(await completionCheck.count(),1);const completionText=await completionCheck.textContent();assert(completionText.includes('提醒'),'非法 canonical 课程完成项必须触发提醒');assert(completionText.includes('5个无法确认的课程完成项'),'必须识别数字字符串、越界ID、null与数组等5个非法完成项');assert(completionText.includes('不会自动删除或改写课程完成记录'),'课程完成记录只能诊断，不得自动清洗');assert.equal(await page.locator('[data-repair]').count(),7);await page.locator('#confirm').check();await page.locator('#repair-btn').click();await page.waitForFunction(()=>document.querySelector('#status')?.textContent.includes('已安全修复7项'));const fixed=await page.evaluate(()=>({profile:JSON.parse(localStorage.getItem('cnc_training_profile_v1')),practice:JSON.parse(localStorage.getItem('cnc_training_practice_v1')),sim:JSON.parse(localStorage.getItem('cnc_training_simulator_v1')),recovery:JSON.parse(localStorage.getItem('cnc_training_data_recovery_v1')),done:localStorage.getItem('cnc_study_completed_v1'),backup:localStorage.getItem('cnc_training_backup_meta_v1'),other:localStorage.getItem('unrelated_keep_me')}));assert.equal(fixed.profile.version,1);assert(!('lastTrainingDate'in fixed.profile));assert.deepEqual(fixed.profile.trainingDays,['2026-08-12','2026-08-12','2026-02-30',null,7,{},[]],'数据健康页不得自动删除或改写异常训练日证据');assert.deepEqual(fixed.profile.badges,['连续训练3天',' 连续训练3天 ',null,{}],'数据健康页不得自动删除重复或非法徽章证据');assert(Array.isArray(fixed.practice.wrongQuestions));assert(Array.isArray(fixed.practice.wrongItems));assert(Array.isArray(fixed.practice.wrong));assert.equal(fixed.practice.wrongQuestions.length,2);assert.equal(fixed.practice.wrongItems.length,1);assert.equal(fixed.practice.wrong.length,1);assert.deepEqual(fixed.practice.lessonScores,{1:100,2:'80',13:90,3:120},'安全修复不得改写异常课程练习成绩');assert.equal(fixed.sim.version,1);assert(fixed.sim.simulators.homing);assert(fixed.recovery.data.cnc_training_profile_v1);assert.equal(fixed.done,originalDone,'安全修复不得改写 canonical 课程完成记录');assert.equal(fixed.backup,JSON.stringify({lastExportAt:'2026-02-30T00:00:00Z'}),'备份元数据只读检查不得改写');assert.equal(fixed.other,'保留');await page.locator('#rollback-btn').click();await page.waitForFunction(()=>document.querySelector('#status')?.textContent.includes('已回滚'));const rolled=await page.evaluate(()=>({profile:JSON.parse(localStorage.getItem('cnc_training_profile_v1')),practice:JSON.parse(localStorage.getItem('cnc_training_practice_v1')),sim:JSON.parse(localStorage.getItem('cnc_training_simulator_v1')),done:localStorage.getItem('cnc_study_completed_v1'),backup:localStorage.getItem('cnc_training_backup_meta_v1'),other:localStorage.getItem('unrelated_keep_me')}));assert(!('version'in rolled.profile));assert.equal(rolled.profile.lastTrainingDate,'2026-02-30');assert.deepEqual(rolled.profile.trainingDays,['2026-08-12','2026-08-12','2026-02-30',null,7,{},[]]);assert.deepEqual(rolled.profile.badges,['连续训练3天',' 连续训练3天 ',null,{}]);assert(!Array.isArray(rolled.practice.wrongQuestions));assert(!Array.isArray(rolled.practice.wrongItems));assert(!Array.isArray(rolled.practice.wrong));assert.deepEqual(rolled.practice.lessonScores,{1:100,2:'80',13:90,3:120},'回滚后异常课程练习成绩必须保持原样');assert(!('version'in rolled.sim));assert(rolled.sim.homing);assert.equal(rolled.done,originalDone,'回滚不得改写 canonical 课程完成记录');assert.equal(rolled.backup,JSON.stringify({lastExportAt:'2026-02-30T00:00:00Z'}),'回滚不得改写备份元数据');assert.equal(rolled.other,'保留');const futureBackup=new Date(Date.now()+3*86400000).toISOString();await page.evaluate(v=>localStorage.setItem('cnc_training_backup_meta_v1',JSON.stringify({lastExportAt:v})),futureBackup);await page.reload();await page.waitForFunction(()=>document.querySelectorAll('#checks .check').length===11);const futureBackupCheck=page.locator('#checks .check').filter({hasText:'备份新鲜度'});assert.equal(await futureBackupCheck.count(),1);assert((await futureBackupCheck.textContent()).includes('晚于当前时间'),'未来备份时间必须提示核对设备时间并重新导出');assert.equal(await page.evaluate(()=>localStorage.getItem('cnc_training_backup_meta_v1')),JSON.stringify({lastExportAt:futureBackup}),'未来备份时间检查不得改写备份元数据');const badDone=JSON.stringify({1:true});await page.evaluate(v=>localStorage.setItem('cnc_study_completed_v1',v),badDone);await page.reload();await page.waitForFunction(()=>document.querySelectorAll('#checks .check').length===11);const badDoneCheck=page.locator('#checks .check').filter({hasText:'课程完成记录'});const badDoneText=await badDoneCheck.textContent();assert(badDoneText.includes('损坏'),'canonical 课程完成根结构异常必须标为损坏');assert(badDoneText.includes('根结构必须为数组'),'canonical 课程完成记录必须保持数组根结构');assert.equal(await page.evaluate(()=>localStorage.getItem('cnc_study_completed_v1')),badDone,'根结构诊断不得改写 canonical 课程完成记录');const badTrainingDays=JSON.stringify({version:1,trainingDays:{a:'2026-08-12'},badges:[]});await page.evaluate(v=>localStorage.setItem('cnc_training_profile_v1',v),badTrainingDays);await page.reload();await page.waitForFunction(()=>document.querySelectorAll('#checks .check').length===11);const badTrainingDayCheck=page.locator('#checks .check').filter({hasText:'训练日记录'});const badTrainingDayText=await badTrainingDayCheck.textContent();assert(badTrainingDayText.includes('损坏'),'trainingDays 非数组必须标为损坏');assert(badTrainingDayText.includes('trainingDays 必须为数组'),'trainingDays 根结构必须保持数组');assert.equal(await page.evaluate(()=>localStorage.getItem('cnc_training_profile_v1')),badTrainingDays,'trainingDays 根结构诊断不得改写学习档案');const min=await page.locator('a:visible,button:visible,input[type="checkbox"]:visible').evaluateAll(nodes=>Math.min(...nodes.map(n=>Math.max(n.getBoundingClientRect().height,n.getBoundingClientRect().width))));assert(min>=44,`touch target ${min}`);assert.equal(errors.length,0,errors.join('\n'));fs.mkdirSync(path.join(root,'cnc/test-results'),{recursive:true});await page.screenshot({path:path.join(root,'cnc/test-results/data-health-390x844.png'),fullPage:true});fs.writeFileSync(path.join(root,'cnc/test-results/data-health.json'),JSON.stringify({checks:10,repairs:7,snapshot:true,rollback:true,strictCalendarDate:true,trainingDayEntryWarns:true,trainingDayShapeRejects:true,preservedTrainingDayEvidence:true,invalidBackupDateWarns:true,futureBackupWarns:true,rewardDuplicateRiskWarns:true,rewardWhitespaceDuplicateWarns:true,badgeInvalidEntryWarns:true,wrongCompatFields:true,practiceScoreWarns:true,canonicalCompletionWarns:true,canonicalCompletionRootRejects:true,preservedCanonicalCompletion:true,preservedRewardRecords:true,preservedUnrelated:true,minTouch:min},null,2));console.log('data health smoke passed')}finally{if(browser)await browser.close();server.kill('SIGTERM')}})().catch(e=>{console.error(e);process.exit(1)});