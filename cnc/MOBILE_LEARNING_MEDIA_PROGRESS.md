# CNC手机学习媒体优化 · 2026-08-09

- 手机首页：降低主标题压迫感，重新平衡副说明、搜索框、快捷查询和练习入口文字层级。
- 学习目录：增大课程缩略图、标题、简介和标签字号，80个小课目录不再依赖9px小字硬塞。
- 小课详情：每课保留主演示图，再增加2张补充图与可暂停/继续的动态教学演示。
- 视频：12个固定关卡各准备1个约6秒、320×180、H.264 MP4工程动画，页面使用本地仓库路径，不依赖临时第三方视频URL。
- PWA：当前受控目标版本 `20260815-pwa46 / 20260815-learning46`；学习媒体页、12个视频、12关主课程数据与G/M代码可信目录继续属于首次安装离线核心。当前版本继续保留“80分只是能力证据、课程真实完成才计入训练奖励”的数据语义，并持续锁定七类安全教学边界：G00快速定位不能假定固定轨迹或把“先Z后XY”当所有机床通用规则；T/H编号相同只是常见工具管理约定，不能仅凭T01后调用H02就直接判错，必须核对实际刀具、工具表/刀补表、当前CNC与机床厂规则及原厂手册；G10属于会改写机床数据的高风险写入，格式、对象、G90/G91解释和权限须按本机原厂手册核对并在写入前备份；G28属于高风险自动参考点返回运动，不能把 `G91 G28 Z0` 或固定“先Z后XY”当成通用防撞保证；G53属于高风险机床坐标定位，不能把 `Z0`、换刀点或固定机械坐标当成跨机床通用安全点；G92在部分铣床/加工中心与部分车床上属于不同语义，坐标类用法与螺纹循环不能跨机型直接互抄；G94在部分铣床/加工中心与部分车床上也存在不同语义，每分钟进给模式与端面/直线车削循环不能跨机型互抄，G93/G94/G95、单位制、F含义、循环地址、起始位置和返回/退刀路径必须按当前CNC与机床厂原厂手册确认。新增G96/G97安全边界：仅在当前CNC与机床厂原厂手册明确支持相应车床语义时，才按恒线速度/固定主轴转速解释；必须确认单位制、S地址含义、本机最高转速限制方式，以及主轴、卡盘、装夹、工件和刀具限制，不得把“G96前必写G50”、固定S数值或“G97后S一定就是转/分”当成跨系统口诀。核心资源变化不通过移出缓存来规避版本门禁。
- AI老师模拟语义：PWA43统一固定13项模拟ID，并按ID合并新版 `records`、旧版 `simulators` 与历史直根记录；90分不得冒充通过，只有真实 `passed === true` 或合法100分才计入通过数。共享手机回归夹具同步使用真实模拟ID，并保留100分通过、90分不通过的明确断言，避免旧夹具掩盖学习路线语义漂移。
- 验收：360×800、390×844检查文字与横向溢出；第10关G41/G42详情检查3张图片解码、真实MP4加载/播放、动态SVG暂停/继续及浏览器错误。
- 自动化：仓库内保留可重复的视频生成脚本，但PR媒体门禁为只读验证；它精确检出PR最新头，对已提交的12个MP4执行ffprobe、系统Chrome真实H.264播放、手机排版、HTTP/浏览器错误和源码未改动检查，不再由CI生成、提交或推送源码。

安全原则：教学参考，需按机床说明书、现场工艺和空运行验证；涉及具体机床参数、报警、刀补和安全步骤时，应核对相同系统与机型的原厂手册并由现场授权人员确认。

## G28参考点返回适用范围
PWA29：G28属于高风险自动运动。G90/G91下中间位置的绝对或增量解释、参考点状态、轴方向与顺序和安全路径必须核对当前CNC和机床厂原厂手册；执行前确认刀具、刀柄、工件、夹具在完整计划运动空间内有安全间隙，并按现场工艺和授权操作规程验证。不要把 `G91 G28 Z0` 或固定“先Z后XY”当成通用防撞规则。

## G53机床坐标定位适用范围
PWA30：G53通常属于当前程序段非模态的高风险机床坐标运动，不是通用安全退刀。机床坐标零点、刀补及其它补偿影响、目标机械坐标、轴行程与完整计划运动空间必须核对当前CNC和机床厂原厂手册；执行前确认刀具、刀柄、工件、夹具有安全间隙，并按现场工艺先做单段、低倍率或空运行验证。不能把 `Z0`、换刀点或固定机械坐标当成跨机床通用安全点。

## G92车铣双语义适用范围
PWA31：G92不是跨机型同一含义。部分铣床/加工中心控制器将其用于工作坐标系偏移或坐标设定相关功能，部分车床控制器则将其用于螺纹车削循环。使用前先确认当前机型、控制器和G92组别；坐标类用法需核对现有G52/G54-G59等偏移、模态状态与设定/清除规则，车床螺纹循环需核对X/Z或U/W、I/Q/F等地址解释、起始位置、主轴与进给同步、退刀或倒角设置及完整运动空间。两类程序不能直接互抄，教学格式示意不得直接作为真实机床参数，必须以当前CNC和机床厂原厂手册、现场工艺及受控验证为准。

## G94车铣双语义适用范围
PWA32：G94不是跨机型同一含义。部分铣床/加工中心控制器把它作为每分钟进给模式，F的单位与含义需要结合G93/G94/G95和当前公制/英制状态确认；部分车床控制器则把G94作为端面/直线车削循环，X/Z或U/W、K/F等地址、起始位置、返回/退刀路径和模态行为没有跨系统统一保证。两类程序不能直接互抄。上机前必须核对当前CNC与机床厂原厂手册、刀补和主轴状态，并确认刀具、刀柄、工件、夹具在完整计划运动空间内有安全间隙；教学示意不得直接作为真实机床参数，首次验证按现场规程使用仿真、图形检查、单段或低风险受控方式。
