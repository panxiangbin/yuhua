你是 2号 AI，负责《数控工程师工作平台》的真实浏览器全流程验收。

项目路径：F:\AI工作台\cnc_param_quickfinder

这是长任务，请用真实浏览器验证，不要只做代码静态检查。

# 背景

当前很多功能只能确认语法通过，不能确认浏览器体验完成。

必须做真实浏览器验收，否则不能说项目可用。

# 允许操作

1. 启动本地服务。
2. 浏览器真实点击。
3. 记录问题。
4. 截图或文字证据。
5. 查看控制台错误。
6. 查看 localStorage。

# 禁止操作

1. 未经确认不要改代码。
2. 不要声称已验证但没有真实证据。
3. 不要跳过移动端。
4. 不要只验证首页。

# 验收范围 A：启动与基础路由

验证：

1. 首页能打开。
2. 工作台能打开。
3. 知识库能打开。
4. 图库能打开。
5. 计算器能打开。
6. 移动端路由不白屏。
7. 刷新页面后不丢失基本状态。

# 验收范围 B：学习卡片

验证：

1. 12 张学习卡片是否显示。
2. 点击卡片是否进入对应详情。
3. 学习卡片样式是否正常。
4. 手机端是否能点击。
5. 是否存在按钮失效。

# 验收范围 C：详情页新增模块

至少测试以下条目：

G54、G02、G03、G41/G42、G43、G81、G83、对刀、回零、转速、进给、程序检查。

每个条目检查：

1. 标题是否正确。
2. 分类是否正确。
3. 摘要是否显示。
4. 快速检查是否显示。
5. 关联工具是否显示。
6. 参数芯片是否显示或合理空态。
7. 智能推荐是否显示。
8. 点击推荐是否切换详情。

# 验收范围 D：关联工具与参数联动

验证：

1. 点击转速计算器工具是否跳转计算器视图。
2. 点击进给计算器工具是否跳转计算器视图。
3. localStorage.cnc_calculator_prefill 是否写入。
4. 从参数芯片跳转时是否带入 toolId。
5. 回到详情页是否不丢 selectedId。

# 验收范围 E：搜索功能

验证关键词：

G02、圆弧、G54、坐标系、刀补、G43、对刀、回零、转速、进给、报警、铝件、粗加工、程序检查。

记录表：

| 关键词 | 是否有结果 | 前3结果是否合理 | 明显错误 | 建议 |
|--------|------------|----------------|----------|------|

# 验收范围 F：移动端验收

至少模拟：

1. 390px 宽度。
2. 430px 宽度。
3. 768px 宽度。

检查：

1. 底部导航是否遮挡内容。
2. 详情页卡片是否过窄。
3. 学习卡片是否能点击。
4. 搜索框是否可用。
5. 推荐卡片是否误触。

# 问题分级

发现问题必须分级：

P0：白屏、核心路由不可用、详情页打不开、搜索崩溃。
P1：核心功能可用但交互错误，如工具跳转失败、推荐点击失败。
P2：显示不理想、样式问题、空态不友好。
P3：文案、间距、细节优化。

# 交付物

请最终生成报告文件名：

CLAUDE_CODE_BROWSER_ACCEPTANCE_ROUND5_20260703.md

报告必须包含：

1. 测试环境。
2. 启动命令。
3. 浏览器类型。
4. 每条测试路径结果。
5. 搜索测试表。
6. 移动端测试表。
7. 问题清单，按 P0/P1/P2/P3 分类。
8. 不能验证的内容必须明确写“无法验证，因此不能确认完成”。

注意：

如果没有真实浏览器操作证据，不能写“已验证”。
