# Cherry Studio 任务 21

这次不要再大范围重做，也不要写“理论上已修复”。

你的任务只有一个核心目标：

**把当前网站做成“手机上可正常切换、可正常进入、首屏不过挤”的可用版本。**

---

## 一、你必须先理解的真实现状

Codex 已经做过真实手机宽度预览，当前情况不是猜测，而是已经验证过：

1. `file:///F:/AI工作台/cnc_param_quickfinder/index.html` 在手机宽度下可以打开
2. 首页已经基本是单列卡片，不是完全坏掉
3. 但是：
   - 打开 `#study-map` 时，页面仍然停留在 dashboard
   - 打开 `#workspace` 时，页面仍然停留在 dashboard
4. 这说明：
   - 当前 hash 路由切换没有真正生效
   - 或者初始视图切换逻辑没有真正执行
5. 另外手机首屏还有明显问题：
   - 主标题过大
   - 信息密度偏高
   - 首屏统计区和说明文字偏挤

---

## 二、本次只允许你处理这 4 件事

### 1. 修复 hash 路由切换

必须保证下面 3 个地址分别打开后，显示的是对应视图，而不是都停在 dashboard：

- `index.html`
- `index.html#study-map`
- `index.html#workspace`

完成标准：

- `index.html` 对应 `view-dashboard`
- `index.html#study-map` 对应学习地图视图
- `index.html#workspace` 对应工作区视图

如果你内部使用的是别的 `view id`，可以不改命名，但最终必须真实切换到对应页面内容。

---

### 2. 修复手机端首屏排版

只处理首页第一屏，目标是“手机上看起来像正式软件入口”。

必须优化这些点：

- 主标题字号缩小
- 副标题行长收紧
- 统计区避免挤压
- 卡片上下留白更均匀
- 顶部导航和首屏内容之间不要显得拥堵

禁止做法：

- 不要为了省事把内容整块删掉
- 不要把模块隐藏到几乎看不见
- 不要只改桌面端而忽略手机端

---

### 3. 保证首页 3 个入口在手机端清晰可点

当前手机首屏至少要把这 3 个入口做清楚：

- 知识库查询
- 新手学习路线
- 图文资料库

要求：

- 卡片为单列
- 点击区域明显
- 文案不要拥挤换行过碎
- 不要出现文字顶边、重叠、超出容器

---

### 4. 不要引入新的空节点报错

你改完后，不能出现新的因为 DOM 取不到元素而导致的前端报错。

重点自查：

- 视图切换初始化
- hash 变化监听
- 移动端菜单点击
- dashboard 统计区
- workspace 初始化
- study map 初始化

---

## 三、你可以改的文件

只允许优先处理这些文件：

- `F:\AI工作台\cnc_param_quickfinder\index.html`
- `F:\AI工作台\cnc_param_quickfinder\app.js`
- `F:\AI工作台\cnc_param_quickfinder\styles.css`
- `F:\AI工作台\cnc_param_quickfinder\styles-enhanced.css`

如果确实必须改别的文件，报告里要说明原因。

---

## 四、你完成后必须提交的证据

新建报告文件：

- `F:\AI工作台\cnc_param_quickfinder\CHERRY_STUDIO_MOBILE_FIX_REPORT.md`

报告里必须写清楚：

1. 你具体改了哪些文件
2. 你修的是哪一段路由逻辑
3. `index.html`、`index.html#study-map`、`index.html#workspace` 分别应该显示哪个视图
4. 你对手机首屏做了哪些排版调整
5. 你有没有删模块，如果删了，删了什么，为什么删

---

## 五、硬性验收标准

只有同时满足下面条件，才算通过：

- 手机端打开 `index.html` 显示 dashboard
- 手机端打开 `index.html#study-map` 真实显示学习地图内容
- 手机端打开 `index.html#workspace` 真实显示工作区内容
- 首页首屏标题和统计区不再明显拥挤
- 3 个主入口卡片在手机端清晰可点
- 不能靠“截图角度”掩盖没切换成功的问题

---

## 六、禁止事项

- 不要只改文案不改逻辑
- 不要提交“理论上已修复”
- 不要再做大而空的全面重构
- 不要碰知识库大数据文件
- 不要改图片库任务，那不是你这轮的工作

这轮你只做：**手机端可用性 + 路由切换修复。**
