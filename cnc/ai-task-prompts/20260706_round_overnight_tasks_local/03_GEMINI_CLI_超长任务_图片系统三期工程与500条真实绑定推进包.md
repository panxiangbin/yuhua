# 3号任务｜超长任务：图片系统三期工程与500条真实绑定推进包

你是 **3号（Gemini CLI）**。  
你之前的强项已经很明确：

- 本地审计
- 图片系统规划
- 提示词批量化
- 文件化产出

但你也有两个持续问题：

1. 有时报告比真实文件更快完成
2. 绑定规则、统计结果、实际 JSON 本体容易不同步

所以这次不再给你“修一个 JSON”这种短任务。  
你这次要做的是：

**把图片系统推进到三期工程级别。**

---

## 一、这次任务的总目标

你要把当前图片系统从“已有批量规划和部分映射”推进到：

**一个可验证、可扩展、可继续批量生产的三期图片工程包。**

核心目标分 4 块：

### 目标A：修好并稳定现有映射主文件

不能再出现：

- 报告说好了
- 主 JSON 还不能稳定解析

### 目标B：把真实绑定规模推进到 500 条以上

注意：

这里不是说“500张图文件”。  
而是说：

**至少 500 条真实、可核查的图片绑定关系。**

### 目标C：把图片提示词继续做成可量产的批次体系

不仅要有 `batch001 / batch002`，还要把下一轮生产继续铺开。

### 目标D：补出自动验证层

以后不能再靠“手工看一眼”和“报告里写通过”。  
你要让下一轮验收可以机器化。

---

## 二、这次必须完成的 7 项工程

### 工程1：修复并稳定 `image-entry-map-round2.json`

这是底线。  
你要确保：

1. 文件可严格解析
2. 不截断
3. 无非法字符串
4. 无坏逗号
5. 无损坏条目

### 工程2：推进真实绑定关系到 500 条以上

你要在现有基础上继续扩充：

- 真实知识点绑定
- 页面区块绑定
- 学习卡片绑定

但必须明确分类。

要求：

- 绑定总数至少 `500`
- 每条绑定必须能解释来源

### 工程3：新增第三批与第四批图片工单

你至少要新增：

1. `image-batch-003-prompts.json`
2. `image-batch-003-prompts.md`
3. `image-batch-004-prompts.json`
4. `image-batch-004-prompts.md`

这两批不能只是占位。  
必须是真正能继续交给 Gemini 出图的工单。

### 工程4：新增缺图队列

你要补出一份真正的缺图队列，例如：

- `image-missing-queue.json`

至少要标出：

1. 哪些知识点还没图
2. 哪些只挂了区块图没有专属图
3. 哪些是高优先级缺图

### 工程5：新增机器化验证脚本

你至少要新建一个脚本，例如：

- `validate-image-system-round3.js`

它至少要检查：

1. 各批次 JSON 可解析
2. 文件名规则合规
3. imageType 枚举合规
4. relatedEntryOrSection 是否存在
5. 绑定级别统计
6. 缺图队列统计

### 工程6：新增三期统计文件

例如：

- `image-binding-stats-round3.json`

必须写清：

1. 总绑定数
2. ENTRY_LEVEL 数量
3. SECTION_LEVEL 数量
4. PLACEHOLDER_LEVEL 数量
5. UNRESOLVED 数量
6. 缺图队列数量
7. 新增批次数量

### 工程7：写一份三期总报告

新建：

- `GEMINI_CLI_IMAGE_SYSTEM_PHASE3_REPORT_20260706.md`

报告必须清楚交代：

1. 修了哪些坏文件
2. 扩了哪些绑定
3. 新增了哪几批图工单
4. 缺图区还有哪些
5. 机器验证做了哪些

---

## 三、你这次必须推进的覆盖范围

你不能只盯着少数几个 G 代码。  
你要推动的绑定覆盖，至少要兼顾这些大类：

1. 学习系统 12 关相关
2. G代码 / M代码
3. 坐标 / 对刀 / 刀补
4. 固定循环
5. 报警 / 故障
6. 参数 / 转速 / 进给
7. 图纸 / 测量 / 公差
8. 材料 / 刀具 / 工艺
9. 加工案例
10. 知识库栏目入口图

---

## 四、这次必须交的体量

最低要求如下：

### 绑定关系

- 至少 `500` 条

### 新批次工单

- `batch003` 至少 `120` 条
- `batch004` 至少 `120` 条

### 缺图队列

- 至少整理出 `100` 条待补图目标

### 统计文件

- 至少 1 份新的 round3 统计

### 验证脚本

- 至少 1 份可跑的验证脚本

这已经不是十几分钟该完成的量。

---

## 五、你这次必须区分清楚的 4 个概念

你报告里必须明确区分：

1. **真实知识点绑定**
2. **页面区块绑定**
3. **学习卡片或功能入口绑定**
4. **尚未完成的缺图预留**

不允许再把这几类混在一起，然后只报一个总数字。

---

## 六、你必须交出的文件

至少包含：

1. `image-entry-map-round2.json`（修好版）
2. `image-binding-stats-round3.json`
3. `image-batch-003-prompts.json`
4. `image-batch-003-prompts.md`
5. `image-batch-004-prompts.json`
6. `image-batch-004-prompts.md`
7. `image-missing-queue.json`
8. `validate-image-system-round3.js`
9. `GEMINI_CLI_IMAGE_SYSTEM_PHASE3_REPORT_20260706.md`

如果你认为需要附加文件，也可以继续加，但必须在报告里列明。

---

## 七、这次必须做的真实验证

你至少要做：

### 验证1：所有核心 JSON 能解析

至少验证：

- round2 主映射
- round3 统计
- batch003
- batch004
- missing queue

### 验证2：字段契约验证

检查：

1. `src`
2. `title`
3. `caption`
4. `relatedEntryOrSection`
5. `bindingLevel`
6. `imageType`

### 验证3：数量统计验证

至少核实：

1. round2 修复后有多少绑定
2. round3 最终有多少绑定
3. 新增多少条
4. 缺图还有多少

### 验证4：抽样验证

至少抽样验证：

1. 10 条 ENTRY_LEVEL
2. 5 条 SECTION_LEVEL
3. 5 条缺图项

---

## 八、你的回复格式

完成后必须以这句开头：

**3号回复：图片系统三期工程推进包已完成**

然后必须写：

1. 你新增的文件
2. 你修复的文件
3. 最终绑定总数
4. batch003 / batch004 各多少条
5. 缺图队列多少条
6. 你做了哪些真实验证
7. 还没解决的问题

---

## 九、最后提醒

你这次最重要的，不是再交一篇“看起来很完整”的文档。  
而是要让下一轮验收时，别人真的能：

1. 解析你的 JSON
2. 读取你的统计
3. 批量接你的提示词
4. 看懂哪些有图，哪些还缺图

**这才叫三期工程，而不是再补一轮说明书。**
