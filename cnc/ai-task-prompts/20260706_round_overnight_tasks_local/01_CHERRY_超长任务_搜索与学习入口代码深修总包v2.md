# 1号任务｜超长任务：搜索与学习入口代码深修总包 v2

你是 **1号（Cherry Studio）**。  
这次不是让你再补一个小函数。  
也不是只修一处接线。

你这次要做的是：

**把“搜索入口 + 学习入口 + 快捷跳转入口”这一整层代码深修一遍。**

---

## 一、这次任务为什么会很长

你上一轮已经暴露了一个问题：

- 你会写方案
- 也会写局部代码
- 但容易把补丁插错层级

所以这次不能再给你“一处返工”就结束。  
我要你直接把 **搜索入口层** 和 **学习入口层** 一起做扎实。

---

## 二、你的总目标

这次你必须围绕下面 4 个区域深修：

### 区域A：工作区搜索入口

包括：

- 搜索框
- 热门词
- 快捷 chips
- 别名词扩展
- 搜索结果命中说明

### 区域B：首页快速启动入口

包括：

- 6 个核心入口卡片
- 快速搜索跳转
- 热门搜索跳转
- 最近查看继续跳转

### 区域C：学习路线入口

包括：

- 新手路线入口
- 12关入口点击逻辑
- 学习入口与知识点跳转关系

### 区域D：搜索行为可解释层

包括：

- 为什么命中
- 命中来自原词还是别名
- 命中来自标题/标签/FAQ/索引

---

## 三、你这次必须完成的 6 项实际工作

### 工作1：彻底清理搜索函数结构

你要全面检查并整理：

- `expandSearchTerm()`
- `matchesKeyword()`
- `scoreEntry()`
- 搜索入口相关调用链

要求：

1. 不允许再出现嵌套污染
2. 同名函数不能重复定义
3. 搜索调用链必须清晰
4. 代码结构必须可读

### 工作2：把别名搜索真正做成“可解释命中”

你不能只让它命中。  
你还要让系统能说明：

1. 这个结果是原词命中的
2. 还是别名扩展命中的
3. 扩展成了哪些词

如果不方便全部显示到 UI，至少要在代码层保留结构，报告里说明。

### 工作3：增强首页快速搜索入口

你要检查和深修：

- 首页搜索框
- 顶部 quick pill
- 热门关键词按钮
- 快速启动卡片跳转到工作区时的默认搜索行为

要求：

1. 从首页搜词能正确进入工作区
2. 点击 `搜 G02 / 搜 1815 / 搜 对刀 / 搜 报警` 这种入口时逻辑正确
3. 不要只是切页面，要把搜索词状态带过去

### 工作4：增强学习入口跳转规则

你要检查：

- 新手路线入口
- 学习卡片入口
- 学习卡片跳到知识点详情的逻辑

你这次至少要补一套更清楚的映射规则结构，避免继续依赖散乱匹配。

### 工作5：增加最小诊断能力

你要给搜索与入口层补一套最小诊断工具，至少能输出：

1. 别名表是否加载成功
2. 热门词按钮是否有 route
3. 学习卡片是否有 target
4. 搜索命中是否能区分来源

### 工作6：写一份真正的代码返工总报告

新建：

- `CHERRY_SEARCH_AND_ENTRY_DEEP_REWORK_REPORT_20260706.md`

这份报告必须清楚交代：

1. 你改了哪些函数
2. 你清理了哪些结构问题
3. 你增强了哪些入口
4. 你验证了哪些行为

---

## 四、你必须覆盖的 5 个高风险场景

你必须把下面这些场景都考虑进去：

### 场景1：新手搜口语词

例如：

- `快移`
- `对刀`
- `刀长`
- `撞机`

### 场景2：用户直接搜代码

例如：

- `G02`
- `G2`
- `M03`
- `G54`

### 场景3：用户从首页卡片点进去

例如：

- 点 `G/M代码`
- 点 `报警排查`
- 点 `参数速查`

### 场景4：用户从学习入口点进去

例如：

- 第1关
- 第2关
- 第6关

### 场景5：搜索无结果或弱结果

你必须考虑：

1. 没有结果怎么办
2. 只有 FAQ 命中怎么办
3. 只有索引命中怎么办

---

## 五、这次你必须交出的文件

至少要交：

1. 改好的 `app.js`
2. 必要时改好的 `search-aliases.js`
3. 如你拆分了结构，可新增：
   - `search-runtime-debug.js`
   - `study-entry-rules.js`
4. 一份总报告：
   - `CHERRY_SEARCH_AND_ENTRY_DEEP_REWORK_REPORT_20260706.md`

---

## 六、这次必须做的验证

至少做下面这些验证，并写进报告：

### 搜索验证

至少验证 10 个词：

- `G2`
- `G02`
- `快移`
- `对刀`
- `撞机`
- `报警`
- `1815`
- `G54`
- `刀补`
- `攻丝底孔`

### 入口验证

至少验证：

1. 首页快速搜索
2. 顶部 quick pill
3. 热门关键词
4. 6 个入口卡片里至少 4 个
5. 学习入口至少 4 个

### 结构验证

至少确认：

1. `matchesKeyword()` 只保留一份有效定义
2. `scoreEntry()` 不再被污染
3. 搜索词状态能跨页面入口流转

---

## 七、你的回复格式

完成后必须以这句开头：

**1号回复：搜索与学习入口深修总包已完成**

然后必须写：

1. 你修改了哪些文件
2. 你新增了哪些文件
3. 你修掉了哪些结构性问题
4. 你完成了哪些入口增强
5. 你做了哪些真实验证
6. 你还不能确认的点

---

## 八、最后提醒

这次不允许再出现：

- 只修一个点
- 只交一篇报告
- 代码插在错误作用域里

这次你要把这一层真正做成：

**别人接手时看得懂、继续改时不容易再炸的入口层代码。**
