# 0号任务｜跨夜超长任务：把静态站真正做成可持续运行的数据软件层

你是 **0号（OPENCODE / DeepSeek 本地可写执行）**。  
这不是一轮“小修小补”。  
这是给你的 **跨夜量级工程任务**。

这次目标非常明确：

**把 `cnc_param_quickfinder` 从“有很多静态内容的网页”推进成“真正能持续运行、持续扩充、持续验证的数据软件层”。**

如果你几个小时就交差，基本等于没按要求做。

---

## 一、这次任务不是做报告，是做一整套可运行的中间层

当前项目已经有很多散落成果：

- `opencode_frontend_ready/`
- `search-aliases.js`
- `frontend-data-layer.js`
- `image-system-round2/`
- `featured-images*.js`
- `gallery-library*.js`
- `data.js`
- `kb-extra.js`
- `knowledge-core-01.js` ~ `knowledge-core-03.js`

但现在的真实状态仍然是：

1. 数据源很多
2. 结构并不统一
3. `file://` 与 `localhost` 的可用性不一致
4. 搜索、FAQ、风险、图片映射、学习路线、知识条目之间还没有形成真正稳定的数据消费层
5. 很多结果“看起来接上了”，但没有形成长期可持续维护的工程结构

所以这次不是让你再补一个 JSON。  
而是让你做：

**一整套“可持续运行的数据软件层”。**

---

## 二、你的总目标

你必须完成下面 5 大块工程：

### 工程A：统一数据源清单与装载入口

你要建立一个真正统一的“数据总入口”，让前端不再到处零散抓数据。

### 工程B：统一搜索消费层

把：

- 原始知识条目
- FAQ 数据
- 搜索建议
- 搜索别名
- 风险关键词
- 相关跳转

这些内容整合成一套真正可消费、可验证的搜索层。

### 工程C：统一图片消费层

把：

- `featured-images*.js`
- `gallery-library*.js`
- `image-system-round2/`

这几套图层数据串成一个能长期扩展的图片层。

### 工程D：统一运行模式兼容层

要明确区分并处理：

1. `file://` 直开
2. `localhost`
3. 将来公网静态部署

三种模式下的数据加载差异。

### 工程E：统一验证与诊断层

以后不能再靠“看起来像是能用”。  
你要补出一套本地可重复跑的诊断能力。

---

## 三、你这次必须交付的实际成果

这次你至少要交出以下内容，缺一项都不算完成：

### 1. 一个统一数据清单

新建或更新一份真正统一的数据清单文件，建议命名：

- `runtime-data-manifest.json`

它至少要列出：

1. 数据源名称
2. 文件路径
3. 类型
4. 加载优先级
5. 用途
6. 是否支持 file 模式
7. 是否支持 localhost 模式
8. 是否支持公网模式

### 2. 一个统一运行时数据装载器

你要给当前站点增加或重构一个统一装载器，例如：

- `runtime-data-loader.js`

它至少要负责：

1. 判断当前运行环境
2. 选择可用的数据加载策略
3. 统一缓存已加载数据
4. 对失败数据做降级
5. 暴露前端可调用接口

### 3. 一套统一搜索聚合层

你要把搜索层真正聚合起来，至少能统一消费这些数据：

- 原始知识条目
- FAQ
- 搜索建议
- 搜索别名
- 风险词
- 相关链接

建议新增：

- `runtime-search-layer.js`

它至少要输出：

1. 搜索建议
2. 归一化后的搜索词
3. 风险命中结果
4. 相关推荐
5. 搜索命中来源说明

### 4. 一套统一图片聚合层

建议新增：

- `runtime-image-layer.js`

它至少要做到：

1. 聚合特色图、图库图、轮播图、学习图
2. 输出统一图片对象结构
3. 输出图片按知识点/栏目/页面区块的关联关系
4. 给出“有图/无图/待补图”状态

### 5. 一套本地诊断工具

必须新增一个真正可跑的诊断脚本，建议命名：

- `scripts/diagnose-runtime-state.js`

它至少要检查：

1. 当前站点引用了哪些数据层脚本
2. 哪些 JSON 能正常解析
3. 哪些数据清单可加载
4. 搜索层是否能产出结果
5. FAQ 是否能成功渲染
6. 风险词是否能命中
7. 图片层是否能返回统一对象
8. file 模式和 localhost 模式差异

### 6. 一份可读的总报告

新建：

- `OPENCODE_RUNTIME_LAYER_OVERNIGHT_REPORT_20260706.md`

这份报告必须不是空泛总结，而是要写：

1. 你改了哪些文件
2. 新增了哪些文件
3. 统一了哪些层
4. 还没统一掉哪些层
5. 本地验证做了哪些
6. 哪些模式仍然不能确认

---

## 四、这次你必须覆盖的 8 个问题

你这次不能挑自己喜欢的做。  
下面 8 个问题都必须覆盖：

### 问题1：搜索来源过散

必须解决：

- 别名一套
- suggestions 一套
- light index 一套
- FAQ 一套
- 原始知识条目又一套

不能继续让前端东拼西凑。

### 问题2：图片来源过散

必须回答：

- 谁是主图源
- 谁是补充图源
- 谁是生成规划图源
- 谁是将来要接 Gemini 出图的入口

### 问题3：运行模式不一致

必须明确：

- `file://` 能做什么
- `file://` 不能做什么
- `localhost` 能做什么
- 将来公网能做什么

### 问题4：失败时没有稳定降级

必须加：

- 失败时 fallback 逻辑
- 占位数据或空状态
- 不让页面一处失败拖全局

### 问题5：没有统一数据对象模型

至少统一三种对象：

1. 条目对象
2. 图片对象
3. 搜索结果对象

### 问题6：没有长期扩展接口

必须让未来新增数据源时，不需要再大改 `app.js`。

### 问题7：没有机器化验证

必须补出脚本化诊断，而不是只靠人工打开页面。

### 问题8：没有形成“后续可交接”的工程边界

必须让后面 `1号 / 3号 / Codex / 2号验收` 都看得懂你的边界。

---

## 五、这次允许改动的文件范围

你可以改：

- `F:\AI工作台\cnc_param_quickfinder\index.html`
- `F:\AI工作台\cnc_param_quickfinder\app.js`
- `F:\AI工作台\cnc_param_quickfinder\styles.css`
- `F:\AI工作台\cnc_param_quickfinder\frontend-data-layer.js`

你可以新增：

- `runtime-data-manifest.json`
- `runtime-data-loader.js`
- `runtime-search-layer.js`
- `runtime-image-layer.js`
- `scripts\diagnose-runtime-state.js`
- `scripts\build-runtime-manifest.js`
- `OPENCODE_RUNTIME_LAYER_OVERNIGHT_REPORT_20260706.md`

如果你还需要新增其他文件，也可以，但必须都放在项目目录下，并在报告里说明用途。

---

## 六、你这次必须做的真实验证

这次任务最关键的不是“做了很多文件”，而是：

**你必须给出真实验证。**

至少要做下面这些验证：

### 验证1：文件存在性验证

确认新文件真实存在

### 验证2：语法级验证

例如：

- JS 能过基础语法检查
- JSON 能成功解析

### 验证3：数据层装载验证

至少验证：

1. suggestions 可读
2. FAQ 可读
3. 风险词可读
4. 图片层可读
5. lookup 可读

### 验证4：运行模式验证

至少区分验证：

1. `file://`
2. `localhost`

如果某一种模式做不到，也必须明确写：

`我已经执行，但无法验证结果，因此不能确认已经完成。`

### 验证5：功能级验证

至少做 8 组查询验证：

- `G02`
- `快移`
- `对刀`
- `报警`
- `1815`
- `G54`
- `刀补`
- `攻丝底孔`

### 验证6：图片层验证

至少验证：

1. 一个知识点能拿到图
2. 一个栏目能拿到图
3. 一个无图条目会返回明确空状态

---

## 七、你的交付物必须拆成 6 块

你最终必须至少交：

1. 运行时数据清单
2. 运行时装载层代码
3. 搜索聚合层代码
4. 图片聚合层代码
5. 诊断脚本
6. 跨夜总报告

如果只交 1-2 个文件，不算完成。

---

## 八、你的回复格式

完成后你必须以这句开头：

**0号回复：运行时数据软件层跨夜任务已完成**

然后必须紧接着给出：

1. 你新增的文件清单
2. 你修改的文件清单
3. 你完成了哪 5 大工程
4. 你做了哪些真实验证
5. 仍不能确认的点

最后必须明确回答：

**这次交付，是不是已经把项目推进成“可持续运行的数据软件层”。**

如果还没有完全做到，也必须老实写还差哪里。

---

## 九、最后提醒

这次任务不是让你继续“补一块”。  
而是要你把这个项目真正往工程化推进一大步。

**这应该是一份做到明天早上都不该轻易交完的任务。**
