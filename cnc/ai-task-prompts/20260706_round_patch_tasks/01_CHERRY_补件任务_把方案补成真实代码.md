# 1号补件任务：把搜索别名方案补成真实代码

你是1号（Cherry Studio）。

你的回复开头必须写：

`1号回复：`

你上一轮交的是方案文档，不是工程交付。
这次不要再写方案，要直接把缺的代码补出来。

你可以读取和修改本地文件。

---

## 一、任务目标

你上一轮文件：

`F:\AI工作台\cnc_param_quickfinder\CHERRY_SEARCH_ALIAS_PLAN_20260706.md`

其中最大问题已经被验收确认：

- 方案有
- 行号也基本对
- 但正文里 0 个代码块
- 没有真正生成 `search-aliases.js`
- 也没有真正改 `app.js`

这次你的目标就是：

把“方案”补成“真实代码补丁”。

---

## 二、必须完成的事

### 任务A：生成真实代码文件

新建：

`F:\AI工作台\cnc_param_quickfinder\search-aliases.js`

要求：

- 至少收录你方案里最核心的那批高频别名
- 不求特别大，但要真实可用
- 不能只写注释，必须有真实导出结构

---

### 任务B：接入现有搜索逻辑

检查并修改当前项目搜索逻辑，让：

- 搜 `G2` 能更稳命中 `G02`
- 搜 `快移` 能命中 `G00`
- 搜 `对刀`、`回零`、`撞机`、`攻丝底孔` 这类别名能更稳命中

要求：

- 尽量小改
- 不要和 0号的数据层冲突
- 如果发现会冲突，要在报告里明确写出来

---

### 任务C：生成代码交付说明

请生成：

`F:\AI工作台\cnc_param_quickfinder\CHERRY_SEARCH_ALIAS_CODE_REPORT_20260706.md`

里面必须写：

- 新建了哪些文件
- 改了哪些文件
- 别名字典有多少组
- 和 0号 的数据层有没有冲突
- 你建议后面保留哪个方案为主

---

## 三、验证要求

你必须自己验证：

1. `search-aliases.js` 真实存在
2. 改完后前端脚本没有明显语法错误
3. 至少给出 5 个可复测的查询样例

---

## 四、回复格式

只汇报：

1. 新建了哪些文件
2. 改了哪些文件
3. 现在能测哪 5 个查询
4. 和 0号 有没有冲突
