# 5号超长任务：搜索语言总包 v3 与高风险检索护栏大扩编

你是5号（Gemini网页版）。

你的回复开头必须写：

`5号回复：`

你不能读取我的本地文件，所以不要假装读取过。

你这次不能再只给“代表性切片”或者“示意样本”。
你要真正产出一个超大体量、可供 Codex 后续切分落库的搜索语言总包。

---

## 一、任务目标

主题：

《数控知识站搜索语言总包 v3》

你要做的不是空泛研究，而是：

- 把真实搜索问法继续大扩容
- 把同义词矩阵继续大扩容
- 把高风险歧义词做得更细
- 把未命中引导做得更全
- 把结果卡模板做得更细分

这次任务必须比上一轮明显更大、更细、更适合直接加工。

---

## 二、必须交付的 6 大部分

### A. 用户真实搜索问法库

你必须给出不少于 `1200` 条搜索问法。

按下面 10 类分组：

1. 新手入门
2. 操机员现场
3. 编程员
4. 工艺员
5. 维修/报警
6. 刀具夹具
7. 材料参数
8. 图纸与测量
9. 参数修改
10. 纯口语/模糊现象描述

每条格式统一为：

`[原始问法] | [问法类型] | [真实意图] | [建议命中分类] | [风险等级]`

要求：

- 必须尽量像真人会搜的话
- 不要全是标准术语
- 要混入口语、错字、现象描述、数字、缩写
- 要覆盖不同系统和不同工种

---

### B. 同义词 / 别名 / 错写 / 英文 / 品牌差异矩阵

你必须给出不少于 `500` 组。

格式统一为：

`标准词 | 简写 | 口语 | 错写 | 英文 | 品牌/系统差异叫法 | 适用分类`

必须重点扩：

- G代码
- M代码
- 坐标
- 回零
- 对刀
- 刀补
- 参数
- 报警
- 刀具
- 材料
- 工艺
- 图纸
- 测量

---

### C. 搜索结果卡模板库

你必须做不少于 `120` 个模板。

按类型分组：

1. G代码
2. M代码
3. 报警
4. 参数
5. 刀具
6. 工艺
7. 材料
8. 学习入口
9. 案例
10. 换算工具
11. 图纸/测量
12. 授权内容预览

每个模板都必须包含：

- 标题结构
- 一句话摘要结构
- 风险提醒结构
- 主按钮文案
- 次按钮文案
- 新手看时先该看什么
- 老手看时先该看什么

---

### D. 搜索未命中引导话术库

你必须给出不少于 `120` 条。

要按情境分组：

- 搜得太短
- 搜错别字
- 搜品牌差异
- 搜纯现象
- 搜口语
- 搜数字报警
- 搜参数号
- 搜工具名
- 搜模糊材料
- 搜图纸符号

每条都要像真实产品文案，不要像论文。

---

### E. 高风险歧义词清单

你必须做不少于 `200` 条。

每条都要包含：

- 关键词
- 风险点
- 为什么危险
- 最容易误导谁
- 结果页必须加什么提示
- 如果做搜索权重，应该怎么特殊处理

这部分必须特别细，不能只讲十几个经典词。

---

### F. 给 Codex 的结构化接入建议

这部分不能只写原则。
你要明确按数据用途拆：

- 哪些最适合做本地词典
- 哪些最适合做模糊搜索
- 哪些最适合做联想词
- 哪些最适合做风险拦截器
- 哪些最适合做结果卡模板
- 哪些最适合做 FAQ 人话翻译层

并且要给出：

- 优先接入顺序
- 为什么这样排
- 如果只接 20%，应该先接哪 20%

---

## 三、强制加码要求

为了避免你又很快“做完”，这次你必须额外附加 3 个附录：

### 附录1：车间真实口语脏样本

至少 `150` 条。
要求尽量接近真实说法，甚至不标准、不完整都行。

### 附录2：最容易搜错的 100 组词

比如：

- “法兰克 / 发那科 / 发兰克”
- “对刀 / 对刀仪 / 刀长补”

你要专门整理这种易错组。

### 附录3：搜索系统最危险的 30 种误命中场景

你要写：

- 错在哪
- 为什么危险
- 系统应该怎么防

---

## 四、输出要求

1. 不要只给示意样本
2. 不要大量重复
3. 不要只讲框架
4. 要像一个能被切分的大型文本资产包
5. 语言要贴数控现场，不要太学院派

---

## 五、最后必须加两段

### 第1段

`5号自评：这次最值得 Codex 先吸收的 5 个模块是什么。`

### 第2段

`5号自评：如果继续做第2轮扩编，我最适合继续补哪3块。`
