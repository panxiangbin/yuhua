你是 6号 AI，负责《数控工程师工作平台》的搜索 P0 优化落地方案。

项目路径：F:\AI工作台\cnc_param_quickfinder

你的任务是做一个长任务，不是简单建议。请连续深入分析 4-6 小时级别的内容，输出可直接给 1号集成的搜索优化报告。

# 背景

当前项目已有搜索功能，但短板是：

1. 搜索结果有命中，但排序不够专业。
2. G代码、M代码、中文术语、现场俗称之间缺少同义词扩展。
3. 搜索结果无法体现数控工程师真实优先级。
4. 后续 AI、知识图谱、学习路径都需要更稳定的搜索基础。

Round4 已经完成数据字段补全和知识图谱去重，详情页增量模块已有数据基础。

# 总目标

设计搜索 P0 权重排序 + 同义词扩展 + 可直接集成的 JS 代码片段 + 测试用例。

# 必须分析的关键词

请逐一分析：

G00、G01、G02、G03、G17、G18、G19、G40、G41、G42、G43、G54、G90、G91、G81、G83、M03、M05、M08、对刀、回零、坐标系、刀补、刀长、半径补偿、圆弧、钻孔、攻丝、宏程序、报警、铝件、不锈钢、粗加工、精加工、进给、转速、线速度、粗糙度。

输出表格：

| 关键词 | 用户真实意图 | 当前可能问题 | 应优先命中的条目类型 | 是否需要同义词 | 推荐权重策略 |
|--------|--------------|--------------|----------------------|----------------|--------------|

# 搜索权重体系

必须给出完整权重表，不能只讲概念。

至少包含：

```js
const SEARCH_WEIGHTS = {
  exactCode: 120,
  titleExact: 100,
  titleStartsWith: 90,
  codeFamilyMatch: 85,
  titleIncludes: 75,
  tagExact: 70,
  tagIncludes: 55,
  categoryExact: 50,
  categoryIncludes: 40,
  synonymExact: 65,
  synonymIncludes: 45,
  summaryIncludes: 30,
  contentIncludes: 15,
  beginnerBoost: 8,
  coreEntryBoost: 12,
  recentEntryBoost: 5
};
```

必须解释：

1. 为什么 G代码精确命中权重要最高。
2. 为什么标题命中要高于正文命中。
3. 为什么同义词命中不能超过精确代码命中。
4. 如何避免“报警”搜出太多无关内容。
5. 如何避免“G1”误伤 G17/G18/G19。

# 同义词表

必须覆盖：

G00 = 快速定位 = 快移 = 空走刀
G01 = 直线插补 = 直线切削 = 走直线
G02 = 顺圆 = 顺时针圆弧 = 顺时针圆弧插补
G03 = 逆圆 = 逆时针圆弧 = 逆时针圆弧插补
G17 = XY平面 = XY平面选择
G18 = ZX平面 = XZ平面选择
G19 = YZ平面 = YZ平面选择
G40 = 取消刀补 = 取消半径补偿
G41 = 左刀补 = 左补偿 = 左半径补偿
G42 = 右刀补 = 右补偿 = 右半径补偿
G43 = 刀长补偿 = 长度补偿 = H补偿
G54 = 工件坐标系 = 工件零点 = 零点偏置
G90 = 绝对编程 = 绝对坐标
G91 = 增量编程 = 相对坐标
G81 = 钻孔循环 = 固定循环
G83 = 深孔钻 = 啄钻 = 排屑钻
M03 = 主轴正转 = 主轴顺转
M05 = 主轴停止 = 停主轴
M08 = 冷却液开 = 开水 = 开切削液
对刀 = 刀具测量 = 设刀长 = 找零点
回零 = 回参考点 = 原点复归 = 机械回零
报警 = 故障 = alarm = 报警代码
进给 = F值 = feed = mm/min
转速 = S值 = rpm = 主轴转速
线速度 = Vc = 切削速度 = surface speed
粗糙度 = Ra = Rz = 表面质量

要求：

1. 同义词表结构适合 JS 直接使用。
2. 考虑中英文大小写。
3. 考虑全角半角、空格、符号归一化。
4. 避免同义词无限扩展导致结果发散。

# 必须提供的 JS 函数

请提供完整代码片段：

```js
normalizeSearchText(value)
expandSearchTerms(keyword)
isExactCodeQuery(keyword)
getEntrySearchText(entry)
scoreEntry(entry, keyword)
rankSearchResults(entries, keyword)
```

要求：

1. 不依赖第三方库。
2. 不改动现有数据结构。
3. 可以最小接入现有 filterEntries()。
4. 不破坏当前详情页渲染。
5. 代码应能通过 node -c app.js。
6. 说明每个函数应放在 app.js 哪个区域附近。

# 测试用例

至少输出 30 个测试用例。

格式：

| 测试关键词 | 预期前3结果类型 | 不能出现在前3的内容 | 验收理由 |
|------------|----------------|--------------------|----------|

必须覆盖：

1. G代码精确搜索。
2. 中文术语搜索。
3. 现场俗称搜索。
4. 英文缩写搜索。
5. 模糊搜索。
6. 容易误伤的短词搜索。

# 交付物

请最终生成报告文件名：

GROK_SEARCH_P0_IMPLEMENTATION_GUIDE_ROUND5_20260703.md

报告必须包含：

1. 关键词诊断表。
2. 权重体系。
3. 同义词表。
4. 完整 JS 代码片段。
5. 集成位置说明。
6. 30 个搜索测试用例。
7. P1 后续建议：拼音首字母、错别字、品牌型号、报警代码搜索。

禁止事项：

1. 不要直接修改源码。
2. 不要引入第三方库。
3. 不要重写整个搜索系统。
4. 不要破坏现有 SPA 路由和详情页固定 DOM。
5. 不要编造不存在的数据字段。
