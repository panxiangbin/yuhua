你是 4号 AI，负责《数控工程师工作平台》的真实 AI 助手后端代理与安全架构设计。

项目路径：F:\AI工作台\cnc_param_quickfinder

这是长任务，不是简单建议。请输出可进入后端实现阶段的完整产品与技术方案。

# 背景

当前明确不做假 AI。

AI 助手未来必须是真实后端能力，服务于：

1. 详情页知识点解释。
2. 加工前检查。
3. 易错点提醒。
4. 参数工具结果解释。
5. 相关知识推荐。
6. 后续程序检查、报警查询、工艺建议。

第一版必须保守、安全、可信。

# 核心原则

1. API Key 不能放前端。
2. 前端不能直接调用 Claude API。
3. 不做模板式假 AI。
4. 不承诺加工安全。
5. 不直接给最终加工参数。
6. 不替用户做最终工艺决策。
7. 不编造知识库不存在的内容。
8. 后端必须做二次安全校验。

# 接口设计

请设计接口：

POST /api/ai/detail-assistant

要求分别给出：

1. 第一版前端请求结构。
2. 后端内部上下文结构。
3. Claude API 请求结构。
4. 后端返回前端的结构化 JSON。
5. 错误响应结构。
6. 被安全规则拦截时的响应结构。

前端第一版建议只传：

```json
{
  "entryId": "learn-g54",
  "question": "G54 设置时最容易出错的地方是什么？",
  "conversation": [],
  "toolContext": null
}
```

后端负责补齐：

1. 当前知识点。
2. relatedEntries。
3. quickCheck。
4. relatedIds。
5. 用户模式。
6. 风险等级。
7. 是否允许 AI 回答。

# 结构化返回 JSON

必须返回结构化结果，不要只返回一段文本。

建议结构：

```json
{
  "answerType": "detail_explanation",
  "conclusion": "",
  "steps": [],
  "risks": [
    {
      "level": "high",
      "text": ""
    }
  ],
  "checklist": [],
  "safetyNote": "",
  "relatedIds": [],
  "blocked": false,
  "blockReason": null,
  "confidence": "medium"
}
```

请说明每个字段用途、前端如何展示、哪些字段必须有兜底。

# 系统提示词

请写一版稳定系统提示词，用于“数控工程师工作平台 AI助手”。

必须包含：

1. 身份定位。
2. 回答边界。
3. 安全禁区。
4. 输出格式。
5. 新手模式与专业模式差异。
6. 知识库依据优先原则。
7. 不知道时如何回答。
8. 对危险问题如何拒答。

不要写成营销话术，要能直接进入后端。

# 安全二次校验规则

后端必须在模型返回后再检查一次。

请给出规则表：

| 风险类型 | 示例 | 处理方式 | 返回给前端的提示 |
|----------|------|----------|------------------|

必须覆盖：

1. 绝对安全承诺。
2. 直接给最终切削参数。
3. 建议跳过试切或首件检查。
4. 要求关闭安全门或绕过保护。
5. 与数控无关问题。
6. 知识库没有依据的问题。
7. 用户要求生成高风险完整程序。

# 内部测试题库

请提供至少 40 个 AI 助手测试问题。

分类：

1. G54 / 坐标系。
2. G43 / 刀长补偿。
3. G02 / G03 圆弧。
4. 对刀与回零。
5. 转速进给。
6. 报警查询。
7. 危险问题拒答。
8. 非数控问题拒答。

每个测试题要给出：

| 问题 | 应该回答 | 不允许回答 | 验收重点 |

# Claude API 接入注意事项

必须说明：

1. API Key 只能在后端。
2. 模型由后端控制。
3. 默认模型使用 claude-opus-4-7。
4. 对复杂问题使用 thinking: { type: "adaptive" }。
5. 系统提示词和安全策略放稳定前缀，便于后续提示词缓存。
6. 不要把整个知识库塞进上下文。
7. 只传当前条目、少量关联条目、最近 3-6 轮对话。
8. 后续需要日志脱敏。

# 交付物

请最终生成报告文件名：

CHATGPT_REAL_AI_BACKEND_ARCH_ROUND5_20260703.md

报告必须包含：

1. 接口设计。
2. 请求 JSON。
3. 响应 JSON。
4. 系统提示词。
5. 安全校验规则。
6. 40 个测试问题。
7. Claude API 接入注意事项。
8. 后续从详情页扩展到程序检查、报警查询、参数解释的路线。

禁止事项：

1. 不要写假 AI 前端代码。
2. 不要给模板回复函数。
3. 不要把 API Key 放前端。
4. 不要让 AI 承诺“不会撞机”。
5. 不要设计一键复制最终参数上机。
