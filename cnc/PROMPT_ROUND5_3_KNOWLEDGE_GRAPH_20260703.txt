你是 3号 AI，负责《数控工程师工作平台》的知识图谱应用化与数据治理方案。

项目路径：F:\AI工作台\cnc_param_quickfinder

这是长任务，请深入审计 data.js 和 knowledge_graph.json，把知识图谱从“有文件”推进到“可被产品使用”。

# 背景

Round4 已经生成或重构了 knowledge_graph.json，并覆盖约 111 个节点、60 条边。

但目前知识图谱还只是数据文件，没有形成产品能力。

本轮目标是让知识图谱能支撑：

1. 详情页智能推荐。
2. 学习路径。
3. 搜索排序增强。
4. AI 助手 relatedEntries。
5. 未来数万篇本地知识库批量导入。

# 允许修改

允许修改：

1. knowledge_graph.json。
2. 报告文件。
3. 可选新增数据质量报告。

谨慎修改：

1. data.js，只允许修复明显错误，不允许大规模重写。

禁止修改：

1. app.js。
2. styles.css。
3. index.html。

# 图谱质量审计

请审计：

1. 每个 node 是否能在 data.js 中找到对应条目。
2. 每条 edge 的 from/to 是否真实存在。
3. 是否有孤立节点。
4. 是否有只有入边没有出边的节点。
5. 是否有只有出边没有入边的节点。
6. 是否有重复边。
7. 是否有关系类型过于单一的问题。
8. 是否有 reason 过短、无意义、不可解释的问题。

输出表格和统计结果。

# 关系类型规范

请制定关系类型，不要只有 related。

至少包含：

prerequisite     前置知识
next             下一步学习
related          相关知识
risk             共同风险
tool             关联工具
compare          易混对比
operation        操作流程关联
parameter        参数计算关联
alarm            报警故障关联
case             加工案例关联

要求：

1. 给出每种关系的定义。
2. 给出适用场景。
3. 给出示例边。
4. 说明详情页、学习路径、搜索如何使用这些关系。

# 核心知识链路

至少围绕以下主题构建可解释链路：

1. 坐标系链路：回零 -> 对刀 -> G54 -> G43 -> 程序检查。
2. 刀补链路：刀具半径 -> G41/G42 -> G40 -> 首件检查。
3. 圆弧链路：G17/G18/G19 -> G02/G03 -> 半径/IJK -> 模拟检查。
4. 钻孔链路：G81 -> G83 -> G84 -> 冷却/排屑 -> 断刀风险。
5. 参数链路：材料 -> 刀具 -> 线速度 -> 转速 -> 进给 -> 粗糙度。
6. 报警链路：报警代码 -> 原因 -> 检查项 -> 维修建议。

每条链路必须说明：

1. 面向什么用户。
2. 为什么这样排序。
3. 详情页应如何推荐。
4. 学习地图应如何引导。

# 图谱驱动产品能力

请给出不改代码的产品方案：

1. 详情页智能推荐如何优先使用图谱。
2. 学习路径如何使用 prerequisite 和 next。
3. 搜索排序如何使用 core 节点和关系距离。
4. AI 助手如何使用 relatedEntries。
5. 未来知识库批量导入时如何自动生成候选边。

# 数据治理规则

请制定 data.js 和 knowledge_graph.json 的长期治理规则。

必须包含：

1. ID 命名规范。
2. 分类规范。
3. 标签规范。
4. quickCheck 写法规范。
5. relatedIds 写法规范。
6. nextId 写法规范。
7. params 写法规范。
8. 图谱边 reason 写法规范。
9. 数据审计脚本未来应检查哪些项。

# 可选修改

如果发现 knowledge_graph.json 中存在明显问题，可以直接修复。

但必须遵守：

1. 不编造 data.js 中不存在的 ID。
2. 不为了凑数量添加无意义边。
3. 所有新增边都必须有 reason。
4. 不改动 UI 和 JS。

# 交付物

请最终生成报告文件名：

GEMINI_KNOWLEDGE_GRAPH_PRODUCTIZATION_ROUND5_20260703.md

报告必须包含：

1. 图谱审计结果。
2. 关系类型规范。
3. 六条核心知识链路。
4. 图谱驱动产品能力方案。
5. 数据治理规则。
6. 已修改文件清单。
7. 未修改但建议后续处理的问题。

禁止事项：

1. 不要改 app.js。
2. 不要改 styles.css。
3. 不要改 index.html。
4. 不要编造不存在的知识点 ID。
5. 不要为了数量添加无意义边。
