# Cherry Studio 交接说明

## 你的角色
你现在是这个数控公网资料站项目的主负责 AI。请基于下面的现状继续统筹，并把后续可执行工作拆给我做。

用户明确要求：
1. Gemini 生成的图片要在网站里明显看得到，不是藏着。
2. 网站不能像一张超长纸往下翻，要像软件一样，用目录树、点一个板块进入下一个板块。
3. 本地数据库的知识点内容要全部尽可能融入公网网址。
4. 访问控制要做到“只有用户想让进的人才能看”，例如抖音粉丝。
5. 不要再让用户自己操作一堆部署和技术步骤，优先由 AI 直接推进。

## 当前公网入口
- 正式公网地址：
  - `https://panxiangbin.github.io/yuhua/cnc/`
- 当前受控访问方式：
  - 主入口打开后会先走访问控制层
  - 也支持授权链接
- 当前可用授权链接示例：
  - `https://panxiangbin.github.io/yuhua/cnc/?invite=xp-cnc-follower-2026`
  - `https://panxiangbin.github.io/yuhua/cnc/?invite=xp-cnc-vip-2026`

## 当前本地项目路径
- 本地开发目录：
  - `F:\AI工作台\cnc_param_quickfinder`
- 公网发布仓库工作目录：
  - `F:\AI工作台\tmp\yuhua_publish_repo`
- 线上 GitHub 仓库：
  - `panxiangbin/yuhua`
- 公网站点挂载目录：
  - `/cnc/`

## 已完成内容

### 1. 网站已不是老式长滚动单页
已经改成多视图软件式结构，核心文件：
- `index.html`
- `app.js`
- `styles.css`

现有主要视图：
- `dashboard`
- `study`
- `workspace`
- `gallery`
- `calculator`
- `library`
- `favorites`
- `access`

当前工作区已经支持：
- 左侧目录树导航
- 默认进入工作区
- 列表模式 / 图文模式切换
- 只看带图条目
- 详情区上一条 / 下一条连续浏览

### 2. 图片已经接入，但离“很明显、很多、很全”还差距离
当前图片相关资产：
- 图库总表：
  - `gallery-library.js`
- 精准绑定图：
  - `featured-images.js`
- 图片目录：
  - `assets/images/...`

当前状态：
- 已有约 `125` 张图片接入站点
- 已做首批重点知识点精准绑定
- 已补自动匹配逻辑，未手工绑定的条目也会尽量按标题、分类、标签、路径自动挂图
- 工作区现在可切图文模式，图片比之前明显得多

但还不够的点：
- 绝大部分知识点还没有做到“一条知识点 = 一组专属图片”
- 现在很多图还是“自动匹配”，不是高质量人工映射
- 用户期待的是明显、海量、直观，接近“打开就像图文资料库”

### 3. 本地知识库已经大量并入公网
当前知识库并入结构：
- 基础结构化条目：
  - `data.js`
  - `kb-extra.js`
- 核心知识包：
  - `knowledge-core-01.js`
  - `knowledge-core-02.js`
  - `knowledge-core-03.js`
- 完整索引分块：
  - `knowledge-full-01.js` 到 `knowledge-full-08.js`
- 原文摘录清单：
  - `kb-content-manifest.js`
- 原文摘录分块：
  - `kb-content-01.js` 到 `kb-content-12.js`

生成脚本：
- `generate_kb_readme_index.py`
- `generate_kb_content_chunks.py`

当前规模：
- 本地知识点索引约 `42235` 条
- 原文摘录已拆成 `12` 个分块，按需加载

当前效果：
- 不只是标题索引
- 详情页已有“知识库正文摘录”区域
- 可以按需继续加载本地原文摘录
- 知识库页新增了分类浏览入口

但还不够的点：
- 仍有大量条目只是“有索引、有摘录”，不是高质量卡片化讲解
- 知识库分类浏览虽然已有，但还不够深，不是完整分层钻取式资料树
- 用户要的是“全部融入”，当前是大规模接入中，但尚未达到最终完工

### 4. 访问控制已有前端可分发方案
当前方案：
- 邀请码 / 授权链接
- 访问控制页已做成分享中心
- 可以复制主入口、授权链接、邀请码

当前优点：
- 用户已经可以把链接发给指定粉丝
- 不再要求用户亲自折腾部署平台

当前不足：
- 仍然是前端级受控访问，不是后端级强权限
- 真正强控制仍需 Cloudflare Access 或后端登录系统
- 现在更像“可控入口”，还不是“强权限内容平台”

## 最近关键提交
发布仓库最近提交：
- `f56b1ec` Default CNC workspace to visual mode
- `ae1ce68` Add CNC library category browser
- `141a7ad` Add CNC visual workspace mode
- `c54da20` Add collapsible CNC navigation tree
- `989995b` Refine CNC workspace navigation flow
- `65aefea` Add CNC image-rich previews and knowledge excerpts
- `2c79983` Polish CNC access sharing center
- `ea3a9c4` Serve CNC full archive in chunked packages

## 当前已验证过的事实
- 公网 `https://panxiangbin.github.io/yuhua/cnc/` 可访问
- 公网图片文件可访问
- 公网知识库分块文件可访问
- GitHub Pages 有同步延迟，但最终都能切到新版本
- 当前用户最强烈的不满点仍是：
  - 图片还不够“扑面而来”
  - 虽然已经软件化不少，但距离“正式软件”还不够

## 你接手后最应该优先推进的方向

### A. 把图片体验做到用户一眼就满意
优先级最高。

建议你统筹我继续做：
1. 批量建立“知识点 -> 专属图片”人工映射，不再主要靠自动匹配
2. 让更多入口默认显示图片，而不是点进详情才看见
3. 继续做“只看带图”专区、图片分类专区、学习图卡专区
4. 让首页、工作区、知识库页都能明显看到图片

### B. 把知识库页做成真正可钻取的目录树
建议你统筹我继续做：
1. 不只是一层分类，而是二级、三级目录
2. 分类点进去后不是简单跳搜索，而是进入子目录视图
3. 像软件资料树一样逐层展开
4. 把高频条目做成精选入口

### C. 把访问控制升级
建议你统筹我继续做：
1. 保留现有邀请码 / 授权链接方案
2. 评估升级到 Cloudflare Access
3. 如果不走 Cloudflare，就设计轻后端登录方案
4. 目标是“用户不用折腾，但权限更强”

## 请你这样给我分工
请不要泛泛而谈，请直接按下面格式给我分配：

1. 当前你判断最该优先完成的 3 个任务
2. 每个任务改哪些文件
3. 先让我做哪一个
4. 做完后你要我回报什么验证结果

## 建议你直接基于这个现状做判断
- 不要把项目再拉回纯静态展示页思路
- 不要再让用户自己做复杂部署
- 默认继续沿着“数控学习软件 / 资料软件 / 图片资料库”的方向推进
- 当前最容易让用户立刻感觉变好的，是：
  - 图片更明显
  - 分类树更深
  - 点击更像软件
  - 访问更像受控资料站

## 给你的直接请求
请你接手主负责，然后：
1. 先快速判断当前版本最缺什么
2. 再给我明确分配下一步编码任务
3. 我继续按你的拆分去做
